"""Placeholder test module for the robotsix-auto-mail bare fixture.

This repository is a test fixture — there is no application code to test.
This file exists solely so that pytest discovers at least one test and
exits with code 0 rather than code 5 ("no tests collected").  It is the
only test file in the repo and always will be.
"""


def test_repo_is_bare_fixture():
    """Assert the one thing we can: this file runs, so the repo is real."""
    assert True
