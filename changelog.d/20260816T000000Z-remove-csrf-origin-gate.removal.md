Remove the component-level CSRF Origin/Host header-matching guard (`_check_csrf`) and the `trusted_origins` allowlist. Per robotsix-standards `component-standard.md`, authentication is centralized — the edge handles cross-site request defence via the SSO session cookie's `SameSite` attribute. The guard repeatedly broke behind the reverse proxy (batch-delete, add-account) and duplicated a defence that belongs upstream.