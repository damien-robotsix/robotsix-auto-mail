Fix Python 2 ``except A, B:`` syntax in ``ConfigVersionStore._read`` (replaced with parenthesized ``except (OSError, json.JSONDecodeError):`` form). Add unit tests for ``discover_accounts_from_settings_stores`` and ``merge_settings_store_accounts`` — the config-recovery path that lets web-UI-added accounts survive a deploy-system overwrite of ``config/config.json``.
