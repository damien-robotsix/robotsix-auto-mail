name: Secret scanning
on:
  push:
    branches: ["**"]
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read

concurrency:
  group: secret-scanning-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scan:
    name: Secret scan
    runs-on: ubuntu-latest
    permissions:
      security-events: write  # needed for SARIF upload from reusable workflow
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920  # v2.20.0
        with:
          egress-policy: audit

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Run gitleaks
        id: gitleaks
        continue-on-error: true
        env:
          WORKSPACE: ${{ github.workspace }}
        run: |
          docker run --rm -v "${WORKSPACE}:/repo" \
            zricethezav/gitleaks:v8.30.1 detect \
            --source /repo \
            --report-path=/repo/gitleaks-report.sarif \
            --report-format=sarif \
            --no-git \
            --verbose

      - name: Upload SARIF to GitHub Code Scanning
        if: steps.gitleaks.outcome != 'skipped'
        uses: github/codeql-action/upload-sarif@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd  # v4
        with:
          sarif_file: gitleaks-report.sarif
          category: gitleaks

      - name: Fail if leaks found
        if: steps.gitleaks.outcome == 'failure'
        run: exit 1
