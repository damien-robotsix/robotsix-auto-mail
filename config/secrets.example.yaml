# Secrets for robotsix-auto-mail.
#
# Copy this file to config/secrets.yaml and fill in your real password.
# config/secrets.yaml is git-ignored so credentials never land in the repo.

mail_password: ""  # fill in your password
