{
  "$defs": {
    "LangfuseConfig": {
      "description": "The canonical ``langfuse`` block: one instance, N named projects.",
      "properties": {
        "host": {
          "default": "",
          "description": "Langfuse instance base URL, e.g. `https://langfuse.example.net`.",
          "title": "Host",
          "type": "string"
        },
        "projects": {
          "additionalProperties": {
            "$ref": "#/$defs/LangfuseProject"
          },
          "description": "One entry per LLM function, keyed by its Langfuse project name. auto-mail has one: `robotsix-auto-mail`.",
          "title": "Projects",
          "type": "object"
        }
      },
      "title": "LangfuseConfig",
      "type": "object"
    },
    "LangfuseProject": {
      "description": "Credentials for one Langfuse project.",
      "properties": {
        "public_key": {
          "default": "",
          "description": "Langfuse project public key (pk-...).",
          "title": "Public Key",
          "type": "string"
        },
        "secret_key": {
          "default": "",
          "description": "Langfuse project secret key (sk-...).",
          "format": "password",
          "title": "Secret Key",
          "type": "string",
          "writeOnly": true
        },
        "project_id": {
          "default": "",
          "description": "Langfuse project id. Only needed by consumers that address the project by id; the component itself never uses it.",
          "title": "Project Id",
          "type": "string"
        }
      },
      "title": "LangfuseProject",
      "type": "object"
    },
    "MailAccount": {
      "description": "One named mailbox: a stable ``account_id`` plus its ``MailConfig``.\n\n``label`` is an optional human-friendly display name.  ``account_id`` is\na stable identifier (e.g. ``\"personal\"``) used in the account's SQLite\nfilename and, later in the epic, in URLs / board selectors \u2014 so it must\nbe non-empty and match ``^[A-Za-z0-9._-]+$``.",
      "properties": {
        "account_id": {
          "description": "Stable identifier for this mailbox, used in filenames and selectors. Must match `^[A-Za-z0-9._-]+$`.",
          "title": "Account Id",
          "type": "string"
        },
        "config": {
          "$ref": "#/$defs/MailConfig",
          "description": "Per-mailbox connection and behaviour settings."
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional human-friendly display name for this account.",
          "title": "Label"
        }
      },
      "required": [
        "account_id",
        "config"
      ],
      "title": "MailAccount",
      "type": "object"
    },
    "MailConfig": {
      "description": "Immutable per-mailbox settings: mail server connection parameters.\n\nSensitive fields (``password``, ``oauth2_token``,\n``oauth2_client_secret``) are typed as :class:`pydantic.SecretStr` so\nthe JSON schema emits ``writeOnly`` and the values are masked in\n``repr`` / ``str``.\n\nLLM and Langfuse credentials are deliberately **not** here: they belong\nto the component, not to a mailbox, and live in the canonical\n``langfuse`` / ``openrouter`` blocks on :class:`MailAccountsConfig`\n(see :mod:`robotsix_auto_mail.config.credentials`).",
      "properties": {
        "imap_host": {
          "description": "IMAP server hostname, e.g. `imap.gmail.com`.",
          "title": "Imap Host",
          "type": "string"
        },
        "smtp_host": {
          "description": "SMTP server hostname, e.g. `smtp.gmail.com`.",
          "title": "Smtp Host",
          "type": "string"
        },
        "username": {
          "description": "Mailbox username (usually the full email address).",
          "title": "Username",
          "type": "string"
        },
        "password": {
          "default": "",
          "description": "Mailbox password or app-specific password. Leave empty when using OAuth2.",
          "format": "password",
          "title": "Password",
          "type": "string",
          "writeOnly": true
        },
        "imap_port": {
          "default": 993,
          "description": "IMAP server port (993 for direct TLS, 143 for STARTTLS).",
          "title": "Imap Port",
          "type": "integer"
        },
        "imap_tls_mode": {
          "default": "direct-tls",
          "description": "TLS negotiation mode for IMAP: `direct-tls` (connect then TLS, default), `starttls` (upgrade after connect), or `none`.",
          "title": "Imap Tls Mode",
          "type": "string"
        },
        "smtp_port": {
          "default": 587,
          "description": "SMTP server port (587 for STARTTLS, 465 for direct TLS).",
          "title": "Smtp Port",
          "type": "integer"
        },
        "smtp_tls_mode": {
          "default": "starttls",
          "description": "TLS negotiation mode for SMTP: `direct-tls`, `starttls` (default), or `none`.",
          "title": "Smtp Tls Mode",
          "type": "string"
        },
        "db_path": {
          "default": "",
          "description": "Path to the per-account SQLite database file. Empty means derive `<mail_data_root>/<account_id>/mail.db`. Relative paths are resolved against mail_data_root.",
          "title": "Db Path",
          "type": "string"
        },
        "imap_folder": {
          "default": "INBOX",
          "description": "IMAP folder to monitor, e.g. `INBOX`.",
          "title": "Imap Folder",
          "type": "string"
        },
        "ingest_interval_minutes": {
          "default": 15,
          "description": "Minutes between automatic ingest cycles when `ingest_mode` is `watch`.",
          "title": "Ingest Interval Minutes",
          "type": "integer"
        },
        "ingest_mode": {
          "default": "once",
          "description": "Ingest behaviour: `once` runs a single poll then exits, `watch` loops forever on `ingest_interval_minutes`.",
          "enum": [
            "watch",
            "once"
          ],
          "title": "Ingest Mode",
          "type": "string"
        },
        "heartbeat_file": {
          "default": "",
          "description": "File touched at the end of each poll cycle in watch mode for Docker HEALTHCHECK. Empty means no heartbeat.",
          "title": "Heartbeat File",
          "type": "string"
        },
        "archive_root": {
          "default": "robotsix-mail-archive",
          "description": "IMAP folder prefix for the self-managed archive hierarchy (e.g. 'robotsix-mail-archive'). This is an IMAP folder name, not a filesystem path \u2014 archived messages are moved between IMAP folders and remain on the mail server.",
          "title": "Archive Root",
          "type": "string"
        },
        "archive_enabled": {
          "default": true,
          "description": "When true, processed messages are moved into the archive; when false they stay in the inbox.",
          "title": "Archive Enabled",
          "type": "boolean"
        },
        "triage_on_ingest": {
          "default": true,
          "description": "When true, the inbox triage agent runs automatically after each ingest cycle.",
          "title": "Triage On Ingest",
          "type": "boolean"
        },
        "triage_guidance": {
          "default": "",
          "description": "Free-text advisory guidance prepended to the triage agent and archive-subfolder proposal for this account. Edited only via PUT /config \u2014 never auto-maintained by the server.",
          "title": "Triage Guidance",
          "type": "string"
        },
        "oauth2_token": {
          "default": "",
          "description": "Static OAuth2 access token for SASL XOAUTH2 authentication. Leave empty for password auth or MSAL-managed tokens.",
          "format": "password",
          "title": "Oauth2 Token",
          "type": "string",
          "writeOnly": true
        },
        "oauth2_client_id": {
          "default": "",
          "description": "OAuth2 client id (application registration id). Only needed for MSAL-managed OAuth2.",
          "title": "Oauth2 Client Id",
          "type": "string"
        },
        "oauth2_client_secret": {
          "default": "",
          "description": "OAuth2 client secret. Only needed for MSAL-managed OAuth2.",
          "format": "password",
          "title": "Oauth2 Client Secret",
          "type": "string",
          "writeOnly": true
        },
        "oauth2_provider": {
          "default": "",
          "description": "OAuth2 provider identifier. Set to `microsoft` for MSAL-managed Microsoft 365 tokens; empty means disabled.",
          "title": "Oauth2 Provider",
          "type": "string"
        },
        "oauth2_tenant": {
          "default": "organizations",
          "description": "Azure AD tenant for MSAL OAuth2, e.g. `organizations`, `common`, or a tenant id.",
          "title": "Oauth2 Tenant",
          "type": "string"
        },
        "log_level": {
          "default": "INFO",
          "description": "Application log level: `DEBUG`, `INFO`, `WARNING`, `ERROR`, or `CRITICAL`.",
          "title": "Log Level",
          "type": "string"
        },
        "log_format": {
          "default": "console",
          "description": "Log output format: `console` (human-readable) or `json` (structured).",
          "title": "Log Format",
          "type": "string"
        }
      },
      "required": [
        "imap_host",
        "smtp_host",
        "username"
      ],
      "title": "MailConfig",
      "type": "object"
    },
    "OpenRouterConfig": {
      "description": "The canonical ``openrouter`` block: one provider key per alias.",
      "properties": {
        "keys": {
          "additionalProperties": {
            "format": "password",
            "type": "string",
            "writeOnly": true
          },
          "description": "The OpenRouter key funding each LLM function, keyed by the same alias as `langfuse.projects` \u2014 that shared alias is what lets billed spend be reconciled against traced spend.",
          "title": "Keys",
          "type": "object"
        }
      },
      "title": "OpenRouterConfig",
      "type": "object"
    },
    "TierModelsConfig": {
      "description": "Per-level model overrides for the three LLM capability levels.\n\nEach field holds a provider-model identifier\n(e.g. ``\"<provider>-<model>\"``) applied to llmio's *default* provider\nslot at that level. Empty means use the llmio default for that level.\nProvider failover (default \u2192 fallback slot) is llmio's concern and is\nnot configured here.",
      "properties": {
        "level1": {
          "default": "",
          "description": "Provider-model identifier for level 1 (cheap, frequent), e.g. `<provider>-<model>`. Empty uses the llmio default.",
          "title": "Level1",
          "type": "string"
        },
        "level2": {
          "default": "",
          "description": "Provider-model identifier for level 2 (workhorse). Empty uses the llmio default.",
          "title": "Level2",
          "type": "string"
        },
        "level3": {
          "default": "",
          "description": "Provider-model identifier for level 3 (frontier, most capable). Empty uses the llmio default.",
          "title": "Level3",
          "type": "string"
        }
      },
      "title": "TierModelsConfig",
      "type": "object"
    }
  },
  "description": "An ordered collection of :class:`MailAccount`s.\n\nOne SQLite DB per account\n-------------------------\nMultiple accounts are modelled as N independent :class:`MailConfig`\ninstances, each carrying its **own** ``db_path``, rather than adding an\n``account_id`` column to every table.  The rationale:\n\n- Per-account state (triage decisions, archive watermarks \u2014 all keyed\n  by ``message_id`` in each DB) is naturally isolated with zero schema\n  migration.\n- Each :class:`MailConfig` already owns a ``db_path`` field, so no new\n  per-row plumbing is required.\n- The cost is one SQLite file per account; uniqueness of ``db_path``\n  across accounts is therefore enforced at load time.\n\nComponent-wide settings\n-----------------------\nThe ``langfuse``, ``openrouter``, ``models``, and per-application level\nfields sit here rather than on each :class:`MailConfig` because they\ndescribe the component's one LLM function, not a mailbox.  Their shape\nis fixed by robotsix-standards \u2014 see\n:mod:`robotsix_auto_mail.config.credentials`.\n\nValidation (all raise :class:`ConfigurationError`): all ``account_id``s\nunique; all ``MailConfig.db_path``s unique across accounts.",
  "properties": {
    "accounts": {
      "description": "Ordered list of mailbox accounts. Each entry has an account_id and its MailConfig.",
      "items": {
        "$ref": "#/$defs/MailAccount"
      },
      "title": "Accounts",
      "type": "array"
    },
    "langfuse": {
      "$ref": "#/$defs/LangfuseConfig",
      "description": "Langfuse tracing configuration: instance host and per-project credentials."
    },
    "openrouter": {
      "$ref": "#/$defs/OpenRouterConfig",
      "description": "OpenRouter provider keys, keyed by the same aliases as langfuse.projects."
    },
    "models": {
      "$ref": "#/$defs/TierModelsConfig",
      "description": "Per-tier model overrides. Each field holds a provider-model identifier; empty uses the llmio tier default."
    },
    "triage_level": {
      "default": 1,
      "description": "Capability level (1-3) assigned to the inbox triage agent. Higher levels use more capable (and expensive) models.",
      "maximum": 3,
      "minimum": 1,
      "title": "Triage Level",
      "type": "integer"
    },
    "classifier_level": {
      "default": 1,
      "description": "Capability level (1-3) assigned to the message classifier agent.",
      "maximum": 3,
      "minimum": 1,
      "title": "Classifier Level",
      "type": "integer"
    },
    "detector_level": {
      "default": 1,
      "description": "Capability level (1-3) assigned to the provider-detection agent.",
      "maximum": 3,
      "minimum": 1,
      "title": "Detector Level",
      "type": "integer"
    },
    "draft_level": {
      "default": 1,
      "description": "Capability level (1-3) assigned to the draft-reply agent.",
      "maximum": 3,
      "minimum": 1,
      "title": "Draft Level",
      "type": "integer"
    },
    "mail_data_root": {
      "default": "/data",
      "description": "Absolute root directory for persisted mail data. Relative db_path values are resolved against this root.",
      "title": "Mail Data Root",
      "type": "string"
    },
    "file_hub_url": {
      "default": "",
      "description": "Base URL of the robotsix-file-hub service (e.g. 'http://file-hub:8080'). Empty means the push-attachments-to-file-hub endpoint is disabled.",
      "title": "File Hub Url",
      "type": "string"
    }
  },
  "required": [
    "accounts"
  ],
  "title": "MailAccountsConfig",
  "type": "object"
}
